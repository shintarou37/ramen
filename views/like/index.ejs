<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ラーメンGO 東京版</title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
  <header>
    <a href="/" class="title head">ラーメンGO 東京版</a>
    <dev>
      <a href="/" class="sign head">ログイン</a>
      <a href="/" class="sign head">新規登録</a>
    </dev>
  </header>
  <div>
    <% for(let i = 0; i < results.length; i++){ %>
      <div style="border: solid;">
        <p><%= results[i].User.name %>さん</p>
        <p><%= results[i].User.createdAt %>さん</p>
        <button style="background-color: red;" onclick="like_click('<%= results[i].User.id %>','<%= results[i].User.name %>')">この人と一緒に行く</button>
      </div >
    <% } %>
    <script>
      function like_click(id, name){
        console.log(id)
        let check = confirm(`${name}さんにいいねしますか？`);
        if(check == true) {
        // OKなら移動
          location.href = `/match`;
        }
      }
      </script>
  </div>
  <footer>
  </footer>
</body>
</html>